---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section
  id={id}
  class={`w-full mx-auto px-4 md:px-6 lg:px-8 max-w-7xl scroll-mt-24 ${className || ''}`}
>
  <slot />
</section>
